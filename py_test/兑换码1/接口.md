# 接口
[[_TOC_]]

### POST /api/exchange 兑换码 兑换

```
@request
{
    code: (string),                     // 兑换码
}

@response
{
    user: {     // 用户公共结构体
        // 
    },
    userCount: {                // 用户统计信息
        coin: (integer),        // 金币数量
        gem: (integer),         // 钻石数量
        integral: (integer),    // 当前经验值
        wealth: (integer),      // 当前财富值
    },
    reward: (string),             // 奖励内容
}

@error
{
    你所在地区暂未开放
}
```
